# Pack Management Examples

This directory contains two tiny packs with the same template id.

Use them to demonstrate:

- multiple packs configured in one project
- active pack selection
- duplicate template ids resolved by the active pack
