id: simple_echo
version: 0.1.0
description: Write a greeting text file into the current working directory.
params:
  name:
    type: str
    required: true
outputs:
  greeting_file:
    path: greeting.txt
run:
  command: >-
    printf 'Hello, %s\n' "${NAME}" > "${LINKAR_RESULTS_DIR}/greeting.txt"
